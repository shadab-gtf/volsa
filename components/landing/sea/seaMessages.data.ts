/** Optional short lines for the koi to approach underwater. Empty keeps the sea wordless. */
export const SEA_MESSAGES: readonly string[] = [];

/** Quiet time after a line dissolves before another appears. */
export const MESSAGE_GAP = 10;
export const MESSAGE_FADE_IN = 0.55;
export const MESSAGE_DISSOLVE = 0.9;
